---
const { text, color = '4b5563' } = Astro.props;
const badgeUrl = `https://img.shields.io/badge/${encodeURIComponent(text)}-${color}?style=for-the-badge&logo=${text.toLowerCase()}`;
---

<img src={badgeUrl} alt={text} class="rounded" />
