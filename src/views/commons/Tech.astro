---
const { label, color } = Astro.props;
const badgeUrl = `https://img.shields.io/badge/${encodeURIComponent(label)}-${color.replace('#', '')}?style=for-the-badge&logo=${label.toLowerCase()}&logoColor=FFFFFF`;
---

<img src={badgeUrl} alt={label} class="rounded" />
